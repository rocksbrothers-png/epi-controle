name: Flutter CI

on:
  push:
    paths:
      - 'flutter/**'
    branches: [main, 'claude/**']
  pull_request:
    paths:
      - 'flutter/**'

defaults:
  run:
    working-directory: flutter

jobs:
  analyze-and-test:
    name: Analyze & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.x'
          channel: stable
          cache: true

      - name: Install Melos
        run: |
          dart pub get
          dart pub global activate melos
          echo "$PUB_CACHE/bin" >> $GITHUB_PATH

      - name: Bootstrap monorepo
        run: melos bootstrap

      - name: Generate l10n
        run: melos run gen:l10n

      - name: Code generation (retrofit, json_serializable)
        run: melos run gen

      - name: Analyze
        run: melos run lint

      - name: Test
        run: melos run test

  build-apk-debug:
    name: Build Android APK (debug)
    runs-on: ubuntu-latest
    needs: analyze-and-test
    if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/heads/claude/')
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: wrapper
          cache-read-only: false

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.x'
          channel: stable
          cache: true

      - name: Install Melos
        run: |
          dart pub get
          dart pub global activate melos
          echo "$PUB_CACHE/bin" >> $GITHUB_PATH

      - name: Bootstrap
        run: melos bootstrap

      - name: Generate l10n
        run: melos run gen:l10n

      - name: Code generation (retrofit, json_serializable)
        run: melos run gen

      - name: Build APK debug
        run: melos run build:apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: epi-admin-debug-${{ github.sha }}.apk
          path: flutter/apps/epi_admin/build/app/outputs/flutter-apk/app-debug.apk
          retention-days: 14

  build-android:
    name: Build Android (AAB)
    runs-on: ubuntu-latest
    needs: analyze-and-test
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.x'
          channel: stable
          cache: true

      - name: Install Melos
        run: |
          dart pub get
          dart pub global activate melos
          echo "$PUB_CACHE/bin" >> $GITHUB_PATH

      - name: Bootstrap
        run: melos bootstrap

      - name: Generate l10n
        run: melos run gen:l10n

      - name: Code generation (retrofit, json_serializable)
        run: melos run gen

      - name: Build AAB
        run: melos run build:android

      - name: Upload AAB
        uses: actions/upload-artifact@v4
        with:
          name: epi-admin-release-${{ github.sha }}
          path: flutter/apps/epi_admin/build/app/outputs/bundle/release/app-release.aab
          retention-days: 30

  build-web:
    name: Build Flutter Web
    runs-on: ubuntu-latest
    needs: analyze-and-test
    if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/heads/claude/')
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.x'
          channel: stable
          cache: true

      - name: Install Melos
        run: |
          dart pub get
          dart pub global activate melos
          echo "$PUB_CACHE/bin" >> $GITHUB_PATH

      - name: Bootstrap
        run: melos bootstrap

      - name: Generate l10n
        run: melos run gen:l10n

      - name: Code generation (retrofit, json_serializable)
        run: melos run gen

      - name: Build Web
        run: melos run build:web

      - name: Upload Web build
        uses: actions/upload-artifact@v4
        with:
          name: epi-admin-web-${{ github.sha }}
          path: flutter/apps/epi_admin/build/web/
          retention-days: 30
