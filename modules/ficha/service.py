"""Serviços de fichas EPI."""


def build_ficha_epi_html(connection, employee_id, actor):
    """Renderiza ficha EPI em HTML.
    Stub — implementação extraída do monolito em fase futura.
    """
    raise NotImplementedError('build_ficha_epi_html não foi extraída ainda do monolito.')


def build_ficha_epi_html_by_period(connection, ficha_period_id, actor):
    """Renderiza ficha EPI por período em HTML.
    Stub — implementação extraída do monolito em fase futura.
    """
    raise NotImplementedError('build_ficha_epi_html_by_period não foi extraída ainda do monolito.')


def ensure_ficha_for_delivery(connection, delivery_row):
    """Garante registro de ficha para uma entrega.
    Stub — implementação extraída do monolito em fase futura.
    """
    raise NotImplementedError('ensure_ficha_for_delivery não foi extraída ainda do monolito.')


def ensure_ficha_for_devolution(connection, devolution_row):
    """Garante registro de ficha para uma devolução.
    Stub — implementação extraída do monolito em fase futura.
    """
    raise NotImplementedError('ensure_ficha_for_devolution não foi extraída ainda do monolito.')
